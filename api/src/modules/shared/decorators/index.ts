import { Permissions } from './permission.decorator';

export { Permissions } from './permission.decorator';
export { AuditLog } from './audit-log.decorator';
export { Webhook } from './webhook.decorator';
export { Feature } from './feature.decorator';
export { User } from './user.decorator';

export const Decorators = [
	Permissions,
]
